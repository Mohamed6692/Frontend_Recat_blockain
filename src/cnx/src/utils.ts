// // utils.ts

// /**
//  * Génère une couleur hexadécimale à partir d'une adresse Ethereum.
//  * @param {string} address - L'adresse Ethereum à partir de laquelle générer une couleur.
//  * @returns {string} - Une couleur hexadécimale.
//  */
// export function generateColorFromAddress(address: string): string {
//     // Utilise le hash de l'adresse pour générer une couleur
//     const hash = address.slice(2, 8); // Prendre une partie de l'adresse pour générer la couleur
//     const color = `#${hash}`;
//     return color;
//   }
  